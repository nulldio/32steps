<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#CC000000">

    <!-- Speaker icon + step count (aligned vertically) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp">

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_speaker"
            android:contentDescription="Volume"
            android:layout_marginEnd="10dp" />

        <TextView
            android:id="@+id/tv_step_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />

    </LinearLayout>

    <!-- Progress bar: gray track + white fill -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="12dp">

        <!-- Gray track (full width = max volume) -->
        <View
            android:id="@+id/tv_progress_track"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="#55FFFFFF" />

        <!-- White fill (scales with current volume) -->
        <View
            android:id="@+id/tv_progress_bar"
            android:layout_width="0dp"
            android:layout_height="2dp"
            android:background="#FFFFFF" />

    </FrameLayout>

</LinearLayout>
